// named export
// return문이 하나일 땐 중괄호와 return 다 생략가능
// 함수 expression (표현식)
// 이 파일안에서만 쓸게아니라 다른곳에서도 쓸거야 export 추가
export const area = r => Math.PI * r * r;
export const circurference = r => Math.PI * r * 2;